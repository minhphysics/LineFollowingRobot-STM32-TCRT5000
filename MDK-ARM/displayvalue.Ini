FUNC void displayvalue (void)
	{
		int i;
		int index;
		//int t;
		exec("log > myvalueblack.log");
		for (i = 0; i < 2000; i++){
			for (index = 0; index < 5; index++){
					printf("%02u,",sensorValues[index]);
		}
		printf("\n");
		for(index=0; index<=350000; index++){};
}
		exec("log off");
	}